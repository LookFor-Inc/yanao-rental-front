/*
const initialState = {
  orderList: []
}

export default (state = initialState, action) => {
  switch (action.type) {
    case ADD_EQUIPMENT:
      return {
        ...state,
        orderList: state.orderList.map(e => {if(e.id===action.payload.id) return}),
      }
    case SET_FLOOR_NUM:
      return {
        ...state,
        num: state.orderList.push(action.payload.num)
      }
    default:
      return state
  }
}
*/
